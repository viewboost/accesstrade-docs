# 03 - Authorization Testing

**Service:** Public API (:3000), Admin API (:3001), File API (:3002)
**Tester:** <!-- Điền tên -->
**Date:** 2026-04-XX

---

| TC ID | WSTG Ref | Tên Test Case | Các bước thực hiện | Kết quả mong đợi | Status | Note |
|-------|----------|--------------|-------------------|------------------|--------|------|
| TC-ATHZ-01 | WSTG-ATHZ-01 | Kiểm tra Directory Traversal / Path Traversal | 1. `http GET "$TARGET_PUBLIC/../../etc/passwd" Authorization:"Bearer $TOKEN_USER"`<br>2. `http GET "$TARGET_PUBLIC/..%2f..%2fetc/passwd"`<br>3. Test trên file service: `http GET "$TARGET_FILE/../../etc/passwd"` | - Server trả 400/404, không trả nội dung file hệ thống<br>- Path traversal sequences bị sanitize | | |
| TC-ATHZ-02 | WSTG-ATHZ-02 | Bypass authorization schema - Horizontal | 1. Login User A, lấy TOKEN_A<br>2. Gọi `GET /notifications/:idOfUserB` với TOKEN_A<br>3. Gọi `GET /events/:id/content/me` với event của User B<br>4. Gọi `GET /user-statistics/:idOfUserB` với TOKEN_A<br>5. Gọi `GET /schedules/:idOfUserB` với TOKEN_A | - Mọi endpoint trả 403 khi access data user khác<br>- Server verify ownership (userId from token == resource owner) | | IDOR - test systematic cho mọi endpoint có :id |
| TC-ATHZ-03 | WSTG-ATHZ-03 | Bypass authorization schema - Vertical (Privilege Escalation) | 1. Dùng TOKEN_USER gọi admin endpoints:<br>  - `GET $TARGET_ADMIN/users`<br>  - `GET $TARGET_ADMIN/events`<br>  - `GET $TARGET_ADMIN/contents`<br>2. Dùng TOKEN_COLLABORATOR gọi root endpoints:<br>  - `GET $TARGET_ADMIN/identifications`<br>  - `GET $TARGET_ADMIN/quick-actions`<br>  - `POST $TARGET_ADMIN/migration`<br>3. Dùng TOKEN_ADMIN gọi root-only endpoints | - User token bị reject trên admin service<br>- Collaborator không access root endpoints<br>- Admin không access root-only endpoints<br>- Rõ ràng 4 tiers: User < Collaborator < Admin < Root | | Roles: IsRoot, IsAdmin, IsCollaborator middleware |
| TC-ATHZ-04 | WSTG-ATHZ-04 | IDOR trên Admin Panel | 1. Login Admin thuộc Partner A<br>2. Gọi `GET $TARGET_ADMIN/users/:userOfPartnerB`<br>3. Gọi `GET $TARGET_ADMIN/events/:eventOfPartnerB`<br>4. Kiểm tra `partner` field trong JWT có enforce boundary không | - Admin Partner A chỉ xem data Partner A<br>- Partner boundary enforced server-side<br>- Xem code `IsAccess()` function | | `middleware/jwt.go:23-25` - IsAccess check |
| TC-ATHZ-05 | WSTG-ATHZ-05 | Cross-service token reuse | 1. Dùng PUBLIC JWT token gọi ADMIN service<br>2. Dùng ADMIN JWT token gọi PUBLIC service<br>3. Dùng PUBLIC JWT token upload file (FILE service)<br>4. Check: 2 service dùng cùng hay khác JWT secret? | - Public token không valid trên Admin service (khác secret)<br>- Admin token không valid trên Public service<br>- File service đúng secret cho đúng role | | `AUTH_SECRET_PUBLIC` vs `AUTH_SECRET_ADMIN` |
| TC-ATHZ-06 | WSTG-ATHZ-06 | File Service Authorization | 1. `http -f POST $TARGET_FILE/photo file@test.jpg` (no auth)<br>2. `http -f POST $TARGET_FILE/admin/photo Authorization:"Bearer $TOKEN_USER" file@test.jpg`<br>3. Kiểm tra xem file service có global auth middleware không | - Upload yêu cầu valid token<br>- User token không access admin upload endpoint<br>- File service không allow unauthenticated requests | | File router không có global JWT middleware |
| TC-ATHZ-07 | WSTG-ATHZ-07 | HTTP Method tampering | 1. Gọi admin endpoint bằng các method khác:<br>  - `OPTIONS $TARGET_ADMIN/users`<br>  - `HEAD $TARGET_ADMIN/users`<br>  - `TRACE $TARGET_ADMIN/users`<br>  - `PATCH $TARGET_ADMIN/users`<br>2. Kiểm tra response - có bypass auth không? | - Tất cả HTTP methods yêu cầu auth<br>- TRACE disabled<br>- Methods không expected trả 405 | | |
| TC-ATHZ-08 | WSTG-ATHZ-08 | Migration endpoint access control | 1. `http POST $TARGET_ADMIN/migration Authorization:"Bearer $TOKEN_COLLABORATOR" key="123123"`<br>2. `http POST $TARGET_ADMIN/migration Authorization:"Bearer $TOKEN_ADMIN" key="123123"`<br>3. Thử key bruteforce nếu không biết key | - Migration endpoint chỉ accessible cho authorized role<br>- Migration key không dễ đoán<br>- Rate limit trên migration endpoint | | KEY_MIGRATION=123123 trong .env |
