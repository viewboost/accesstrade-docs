# 02 - Authentication Testing

**Service:** Public API (:3000), Admin API (:3001), File API (:3002)
**Tester:** <!-- Điền tên -->
**Date:** 2026-04-XX

---

| TC ID | WSTG Ref | Tên Test Case | Các bước thực hiện | Kết quả mong đợi | Status | Note |
|-------|----------|--------------|-------------------|------------------|--------|------|
| TC-ATHN-01 | WSTG-ATHN-01 | Kiểm tra truyền tải credentials qua kênh mã hóa | 1. Bật Burp/ZAP proxy, thực hiện login (OAuth Google, TikTok, OTP)<br>2. Kiểm tra tất cả request chứa token/credentials<br>3. Xác nhận HTTPS được dùng cho mọi request chứa JWT | - Tất cả credentials/tokens truyền qua HTTPS<br>- Không có mixed content<br>- Header `Strict-Transport-Security` bật | | |
| TC-ATHN-02 | WSTG-ATHN-02 | Kiểm tra tài khoản mặc định | 1. Thử login admin: `admin/admin`, `root/root`, `test/test`<br>2. Kiểm tra có user mặc định trong MongoDB seed data<br>3. Xem KEY_MIGRATION có dễ đoán không (hint: `123123` trong .env) | - Không có default credentials<br>- Migration key không phải giá trị dễ đoán | | |
| TC-ATHN-03 | WSTG-ATHN-03 | Kiểm tra cơ chế khóa tài khoản | 1. Đăng nhập sai 10 lần liên tiếp cho cùng account<br>2. Ghi nhận response mỗi lần (có thay đổi không?)<br>3. Kiểm tra OTP verify: gửi sai 10+ lần liên tiếp | - Account bị khóa tạm sau N lần sai<br>- Response chung chung, không tiết lộ account tồn tại<br>- OTP có rate limit | | |
| TC-ATHN-04 | WSTG-ATHN-04 | Bypass JWT authentication | 1. `http GET $TARGET_PUBLIC/users/me` (no header)<br>2. `http GET $TARGET_PUBLIC/users/me Authorization:""`<br>3. `http GET $TARGET_PUBLIC/users/me Authorization:"Bearer"`<br>4. `http GET $TARGET_PUBLIC/users/me Authorization:"Bearer "`<br>5. `http GET $TARGET_PUBLIC/users/me Authorization:"Bearer invalid.token"`<br>6. Thử các protected endpoints admin/file service tương tự | - Mọi endpoint protected trả 401 khi không có token hợp lệ<br>- Skipper logic không cho phép bypass<br>- Empty/malformed Bearer bị reject | | Xem code: `middleware/jwt.go:31-36` - Skipper skip khi header = "" hoặc "Bearer" hoặc "Bearer " |
| TC-ATHN-05 | WSTG-ATHN-05 | Kiểm tra token reuse across devices | 1. Login lấy JWT token từ device/browser A<br>2. Copy token sang device/browser B<br>3. Gọi API `/users/me` từ device B với token copy | - Token bound theo device/session<br>- Hoặc: chấp nhận reuse nhưng có revocation mechanism | | |
| TC-ATHN-06 | WSTG-ATHN-06 | JWT Algorithm Confusion & None Attack | 1. `jwt_tool $TOKEN_USER -X a` (none algorithm)<br>2. `jwt_tool $TOKEN_USER -X k` (key confusion HMAC/RSA)<br>3. Decode token xem algorithm: `jwt_tool $TOKEN_USER`<br>4. Check `golang-jwt/jwt` version (v3 có bug, v4 safe) | - Server reject token với alg: none<br>- Server reject token với algorithm khác expected<br>- Sử dụng golang-jwt v4+ (không phải v3) | | |
| TC-ATHN-07 | WSTG-ATHN-07 | JWT Secret Brute Force | 1. `jwt_tool $TOKEN_USER -C -d rockyou.txt`<br>2. Thử common secrets: `secret`, `password`, `jwt_secret`, tên project<br>3. Nếu tìm được secret → tạo token giả với `isRoot: true` | - JWT secret đủ mạnh (>= 256 bit entropy)<br>- Không thể brute force trong thời gian hợp lý | | |
| TC-ATHN-08 | WSTG-ATHN-08 | JWT Claims Manipulation | 1. Decode token xem claims: `_id`, `name`, `isRoot`, `partner`, `type`<br>2. Nếu biết secret: modify `isRoot: true`, sign lại, gọi admin endpoints<br>3. Modify `_id` thành user khác, gọi `/users/me`<br>4. Modify `partner` thành partner khác | - Server verify claims against database (không trust JWT claims blindly)<br>- Modified token bị reject hoặc server re-validate từ DB | | |
| TC-ATHN-09 | WSTG-ATHN-09 | Google OAuth Flow Testing | 1. Bắt đầu Google OAuth flow, intercept redirect<br>2. Kiểm tra `state` parameter có tồn tại (CSRF protection)<br>3. Modify `redirect_uri` thành domain khác<br>4. Thử replay authorization code | - Có state parameter để chống CSRF<br>- redirect_uri validated server-side (whitelist)<br>- Authorization code single-use | | |
| TC-ATHN-10 | WSTG-ATHN-10 | TikTok OAuth Flow Testing | 1. Bắt đầu TikTok OAuth flow<br>2. Kiểm tra state parameter<br>3. Kiểm tra redirect_uri validation<br>4. Check scopes requested: `open_id, union_id, avatar_url, display_name, username` | - State parameter present<br>- Redirect URI validated<br>- Minimal scopes requested | | |
| TC-ATHN-11 | WSTG-ATHN-11 | OTP Brute Force & Bypass | 1. Request OTP verify, gửi sai OTP 20 lần<br>2. Kiểm tra có rate limit/lockout không<br>3. Thử OTP đã dùng (replay)<br>4. Thử OTP format: 4-digit? 6-digit? có expiry? | - Rate limit sau 5 lần sai<br>- OTP single-use, expire trong 5-15 phút<br>- Không thể brute force 4-6 digit OTP | | |
| TC-ATHN-12 | WSTG-ATHN-12 | Webhook Authentication Testing | 1. `http POST $TARGET_PUBLIC/content-callback` (no auth headers)<br>2. `http POST $TARGET_PUBLIC/opshub-webhook` (no X-Api-Key)<br>3. `http POST $TARGET_PUBLIC/opshub-webhook X-Api-Key:"wrong"` | - Content callback yêu cầu authentication<br>- OpsHub webhook reject khi thiếu/sai API key<br>- Không có webhook endpoint open | | Xem code: `routeauth/auth.go:33-41` - AuthWebHook commented out |
