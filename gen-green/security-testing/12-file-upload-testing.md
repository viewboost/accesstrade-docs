# 12 - File Upload Testing

**Service:** File API (:3002)
**Tester:** <!-- Điền tên -->
**Date:** 2026-04-XX

> **NOTE:** VCreator có dedicated File service (port 3002) với upload photo, admin upload, video processing (FFmpeg). Category này riêng biệt vì attack surface lớn.

---

| TC ID | WSTG Ref | Tên Test Case | Các bước thực hiện | Kết quả mong đợi | Status | Note |
|-------|----------|--------------|-------------------|------------------|--------|------|
| TC-UPLD-01 | WSTG-BUSL-08 | File Extension Bypass | 1. Upload file `.php`, `.jsp`, `.sh`, `.exe`<br>2. Upload double extension: `test.php.jpg`, `test.jpg.php`<br>3. Upload with null byte: `test.php%00.jpg`<br>4. Upload case variation: `test.PHP`, `test.PhP` | - Only allowed extensions accepted (jpg, png, gif, webp...)<br>- Double extensions rejected<br>- Null bytes stripped<br>- Case-insensitive extension check | | `ValidateExtension()` in `model/mg/file.go:72` |
| TC-UPLD-02 | WSTG-BUSL-08 | Content-Type Mismatch | 1. Upload `.html` file with Content-Type: `image/jpeg` (Burp intercept)<br>2. Upload executable with Content-Type: `image/png`<br>3. Upload SVG with embedded JavaScript<br>4. Check: server validate magic bytes hay chỉ extension? | - Server validate file content (magic bytes), không chỉ extension<br>- Content-Type mismatch rejected<br>- SVG scripts neutralized | | UploadSingle chỉ check extension, KHÔNG check magic bytes |
| TC-UPLD-03 | WSTG-BUSL-08 | File Size Validation | 1. Upload file > max allowed size<br>2. Check: server reject trước hay sau khi write to disk?<br>3. Upload nhiều large files liên tiếp (resource exhaustion)<br>4. `dd if=/dev/zero of=/tmp/big.jpg bs=1M count=500` → upload | - Size validated TRƯỚC khi write to disk<br>- Clear error message cho oversized files<br>- No disk exhaustion from repeated uploads | | `ValidMaximumSize()` nhưng gọi SAU khi file đã write |
| TC-UPLD-04 | WSTG-BUSL-08 | ZIP Slip Attack | 1. Tạo malicious ZIP:<br>```python<br>import zipfile<br>with zipfile.ZipFile('evil.zip', 'w') as z:<br>    z.writestr('../../tmp/proof.txt', 'PWNED')<br>```<br>2. Upload qua endpoint nhận ZIP<br>3. Kiểm tra file có extracted ngoài upload dir không | - ZIP entry paths sanitized<br>- Không extract ngoài upload directory<br>- Path traversal sequences stripped | | CRITICAL: `upload_file.go:176-210` - không validate zip entry paths |
| TC-UPLD-05 | WSTG-BUSL-08 | ZIP Bomb | 1. Tạo ZIP bomb (small file, massive uncompressed):<br>```python<br>import zipfile<br>with open('/tmp/bomb.txt', 'wb') as f:<br>    f.write(b'A' * (100*1024*1024))<br>with zipfile.ZipFile('/tmp/bomb.zip', 'w', zipfile.ZIP_DEFLATED) as z:<br>    z.write('/tmp/bomb.txt')<br>```<br>2. Upload ZIP bomb<br>3. Monitor server memory/disk usage | - Server check uncompressed size trước extract<br>- Reject ZIP bombs<br>- No OOM or disk exhaustion | | |
| TC-UPLD-06 | WSTG-BUSL-08 | Filename Injection | 1. Upload với filename chứa path traversal: `../../../etc/test`<br>2. Upload với filename chứa shell chars: `file;id.jpg`, `` file`whoami`.jpg ``<br>3. Upload với filename rất dài (500 chars)<br>4. Upload với Unicode filename | - Filenames sanitized (VCreator generate new name từ timestamp - tốt)<br>- Original filename stored nhưng không dùng cho file path<br>- Shell characters neutralized | | `ptime.GenerateNameFileFromTimeNow()` - good practice |
| TC-UPLD-07 | WSTG-BUSL-08 | Upload without Authentication | 1. `http -f POST $TARGET_FILE/photo file@test.jpg` (no auth)<br>2. `http -f POST $TARGET_FILE/admin/photo file@test.jpg` (no auth)<br>3. `http -f POST $TARGET_FILE/video file@test.mp4` (no auth) | - All upload endpoints require valid JWT<br>- No anonymous uploads<br>- 401 response for unauthenticated requests | | File router: chỉ per-route auth, không global middleware |
| TC-UPLD-08 | WSTG-BUSL-08 | Uploaded File Access Control | 1. Upload file (private), lấy URL<br>2. Access URL từ browser khác (không login)<br>3. Kiểm tra MinIO presigned URL expiry<br>4. Thử modify presigned URL params (change filename/bucket) | - Private files chỉ access qua presigned URL<br>- URL expire sau 10 minutes<br>- Tampered URLs rejected<br>- Public files có CDN URL nhưng không chứa PII | | MinIO presigned: 10 min expiry |
| TC-UPLD-09 | WSTG-BUSL-08 | Video Upload & FFmpeg Security | 1. Upload video file chứa malicious metadata<br>2. Upload file giả dạng video nhưng không phải<br>3. Kiểm tra FFmpeg command injection via filename/metadata<br>4. Upload very long video (resource exhaustion) | - FFmpeg processing sandboxed<br>- Malicious metadata stripped<br>- No command injection via file input<br>- Processing timeout enforced | | `ffmpeg-go` dependency |
| TC-UPLD-10 | WSTG-BUSL-08 | Concurrent Upload Race Condition | 1. Upload cùng 1 file tên 10 lần đồng thời<br>2. Kiểm tra file naming collision<br>3. Check disk cleanup hoạt động đúng | - No filename collision (timestamp-based naming)<br>- All temp files cleaned up<br>- No orphaned files on disk | | |
